{
  "scripts": {
    "prepare-client": "cd client && npm install && npm run build",
    "prepare-server": "cd server && npm install",
    "start": "cd server && npm install && cd ../client && npm install && npm run build",
    "heroku-postbuild": "cd server && npm install && cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.6",
    "@babel/core": "^7.24.6",
    "@babel/plugin-proposal-nullish-coalescing-operator": "^7.18.6",
    "@babel/preset-env": "^7.24.6",
    "@babel/preset-react": "^7.24.6"
  }
}